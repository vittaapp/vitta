name: vitta_app
description: "Proyecto Vitta Salud"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.3.1
  shared_preferences: ^2.5.5
  image_picker: ^1.1.2
  file_picker: ^8.1.4
  firebase_core: ^4.6.0
  firebase_auth: ^6.3.0
  cloud_firestore: ^6.2.0
  geolocator: ^13.0.0
  firebase_messaging: ^16.1.3
  flutter_riverpod: ^3.3.1
  go_router: ^17.1.0
  google_sign_in: ^7.2.0
  google_maps_flutter: ^2.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true